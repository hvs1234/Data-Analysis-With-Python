# We can use dot product of two vectors using the np.dot function
import numpy as np
# kanto = np.array([73,67,43])

# w1,w2,w3 = 0.3,0.2,0.5
# weights = np.array([w1,w2,w3])

# print(f"{np.dot(kanto,weights)}")
# print(f"{(kanto*weights).sum()}")

arr1 = list(range(1000000))
arr2 = list(range(1000000,2000000))

print(np.array(arr1))
print(np.array(arr2))